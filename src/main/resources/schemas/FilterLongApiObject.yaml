title: LongFilterApiRequest
type: object
properties:
  onValue:
    type: integer
  minValue:
    type: integer
  maxValue:
    type: integer
  action:
    type: string
    enum:
      - EQUAL
      - GREATER_EQUAL
      - LESS_EQUAL
      - BETWEEN
      - IS_NULL
